# 任务：角色原图自动拆分为多部位图层并合成PSD

## 背景与目标

我有一张角色立绘原图，需要把它拆分成若干个"部位图层"（如头发、脸部、眼睛、身体、衣服、配饰等），每个部位输出为**与原图同尺寸、同坐标系**的透明背景PNG，最后再用脚本把这些PNG按正确的图层顺序合并成一个多图层PSD文件，叠加后必须与原图完全一致。

请帮我搭建一套**可重复运行**的本地自动化流程（Python脚本 + Photoshop脚本），而不是一次性手工操作。

---

## 第一部分：部位拆分（图像处理脚本）

### 输入
- 一张角色原图（PNG/JPG，分辨率为 `W x H`，路径由我提供）
- 一份部位列表（我会告诉你具体要拆哪些部位，数量不固定，可能是5个也可能是20多个，不要硬编码25这个数字，做成可配置）

### 核心约束（最重要，必须严格遵守）
1. **画布尺寸统一**：每个部位输出的PNG画布尺寸必须和原图完全一致（`W x H`），部位内容放在它在原图中所在的像素位置，**不要**裁剪到部位最小外接矩形再单独保存（那样会丢失坐标信息，导入PS后位置会错）。
2. **透明背景**：非该部位的区域必须是完全透明（alpha=0），不能是白色或其他底色。
3. **像素级还原优先**：优先用分割/抠图的方式从原图中直接抠出该部位的真实像素（保留原图的颜色、光影、笔触），而不是用生成式AI重新"画"一个新的部位图——重新生成容易导致颜色、形状、线稿风格跟原图不一致。
4. **遮挡区域如何处理**：如果某部位被其他部位遮挡（比如刘海遮住部分眼睛、衣领遮住部分脖子），请明确询问我希望如何处理，给我以下选项，不要自行决定：
   - 选项A：被遮挡部分保持空白/透明（不做任何补全）
   - 选项B：用inpainting/outpainting补全被遮挡区域（需要额外调用图像生成模型，效果不保证完美）
   - 选项C：遮挡部分直接复制相邻图层像素做简单延展（不追求语义正确，只追求图层独立可编辑）

### 处理流程建议
1. 用分割模型（如 SAM、人物部件分割模型，或如果只能用简单方法，则用颜色聚类/手动mask辅助）为每个部位生成mask
2. 对每个mask做适当的羽化/边缘处理，避免抠图边缘出现锯齿或硬边
3. 按mask从原图中抠取像素，输出到统一尺寸的透明画布上
4. 检查所有部位图层叠加后是否能完整还原原图（生成一张"叠加校验图"和原图做diff对比，差异过大要报错并列出问题区域）

### 输出规范
- 输出目录结构：
  ```
  output/
    01_背景.png
    02_身体.png
    03_衣服.png
    04_头发后层.png
    05_脸.png
    06_眼睛.png
    07_头发前层.png
    ...
  ```
- **文件名前缀数字 = 图层顺序**（数字越小越在底层，越大越在顶层），这个顺序我会单独提供给你，请按我给的顺序命名，不要自己猜测前后关系
- 额外输出一份 `layers_manifest.json`，记录：
  ```json
  {
    "canvas_width": W,
    "canvas_height": H,
    "layers": [
      {"order": 1, "filename": "01_背景.png", "name": "背景"},
      {"order": 2, "filename": "02_身体.png", "name": "身体"}
    ]
  }
  ```
  这个manifest后续PS脚本会读取，用于确定图层顺序和命名，避免脚本里硬编码文件名。

---

## 第二部分：PSD合并脚本（Photoshop自动化）

### 要求
用 **ExtendScript (.jsx)** 写一个脚本（不要用UXP插件，我需要的是双击运行/通过 File > Scripts > Browse 运行的传统脚本），功能如下：

1. 读取 `output/layers_manifest.json`
2. 创建一个新文档，画布尺寸 = manifest中的 `canvas_width x canvas_height`
3. 按 `order` 从小到大依次：
   - 打开对应PNG文件
   - 将其内容复制为新图层，放入主文档，图层放在 (0,0) 位置（因为每张PNG画布已经和主文档一致，不需要计算偏移）
   - 设置图层名称为 manifest 中的 `name`
   - 关闭临时打开的源文件，释放内存
4. 调整图层顺序，确保 `order` 数字大的图层在图层面板中位于上方（更靠近顶层）
5. 全部图层导入完成后，**自动执行一次"合并可见图层并对比原图"的校验**（可选：把合并结果导出一张flatten PNG，方便我和原图做肉眼比对）
6. 将最终PSD保存到指定路径（路径作为脚本顶部的可配置变量，不要写成绝对路径硬编码在脚本逻辑里）
7. 脚本运行过程中在Photoshop的JS Console（或alert）输出处理进度日志（"正在处理第X/Y个图层：xxx"），方便我排查哪一步出错

### 边界情况处理
- 如果 `layers_manifest.json` 中某个文件在output目录里找不到，脚本要弹出明确报错并停止，不要静默跳过
- 如果PNG尺寸和manifest里声明的canvas尺寸不一致，要报错提示

---

## 交付物清单

请最终交付：
1. 一个Python脚本（部位拆分 + manifest生成），可通过命令行运行，参数至少包括：原图路径、部位配置、输出目录
2. 一份 `layers_manifest.json` 的示例
3. 一个 `merge_layers.jsx` 脚本
4. 一份简短的使用说明（README），说明运行顺序、依赖安装方式（如果用到SAM等模型，注明模型权重下载方式）

## 在开始写代码前，请先回答我以下问题，确认理解一致后再动手

1. 部位之间的遮挡关系处理，你倾向选项A/B/C中的哪一种？（见上文"遮挡区域如何处理"）
2. 是否已有现成的分割模型/工具链可用，还是需要从零搭建分割流程？
3. 原图是单角色还是可能存在多角色/背景干扰，是否需要先做角色主体抠出这一步？
