# Agent Pet 项目锐评、完整改造建议与 48 小时 Demo 上线方案

> 审计日期：2026-07-10
> 审计范围：用户提供的 22 张前端截图、当前 E:\agentproject 源码、LangChain/LangGraph/多 Agent 运行链、现有自动化测试与静态检查
> 结论口径：截图只能证明“看起来存在”，代码与本次测试才证明“当前真的存在”。未运行 live provider gate，因此不把真实供应商可用性算作本次已验证。

---

## 一、先说结论：现在这不是完整产品，是一个功能很多、主线很弱、技术叙事还说过头的工程展厅

最狠也最准确的一句话是：

**首页像产品，聊天像调试器，记忆像后台模板，设置像开发者控制台，多 Agent 则像一块已经装上仪表盘、但发动机还没有真正接通的招牌。**

你不是没做东西。恰恰相反，你做得太多：聊天、记忆、日记、资料库、图谱、任务、Wiki、TTS、自动整理、模型分工、回顾、衰减、热力图、活动账本，全都往一个桌宠产品里塞。结果是每个模块都“看起来有”，但用户真正能顺畅完成的闭环反而不够突出。

当前版本若直接按“完整版”上线，不叫勇敢，叫公开验尸。用户会在五分钟内看出三个事实：

1. 很多页面是占位入口或半成品；
2. 页面边界和滚动契约没有稳定下来；
3. 产品把技术复杂度直接甩给了用户。

但它并非没有救。相反，这个项目的后端安全边界、本地优先、SQLite/FTS/Vault 主路径、SSE 事件和 LangGraph 基础链都比一般 Demo 扎实。你缺的不是继续加功能，而是砍掉噪音、接通一条真的多 Agent 旗舰链、把前台重新压缩成三个可信动作：

- 和它聊；
- 让它找回并引用记忆；
- 让它替你完成一个可追踪、可确认、可撤销的动作。

### 当前健康度

| 维度 | 评分 | 毫不留情的判断 |
| --- | ---: | --- |
| 产品方向 | 7/10 | 本地优先、陪伴、可审计记忆有差异化，不是换皮聊天壳 |
| 视觉辨识度 | 6/10 | 首页角色与夜景有品牌感，离开首页立刻退化成通用暗色后台 |
| 信息架构 | 3/10 | 按技术模块铺页面，不按用户任务组织 |
| 交互与容器稳定性 | 3/10 | 常用窗口尺寸就出现遮挡、双滚动和底栏压内容 |
| 前端可维护性 | 2/10 | 394 KB、约 1.55 万物理行的 desktop-polish.css 已是补丁考古现场 |
| LangChain/LangGraph 真实度 | 7/10 | 真用了、主链也真跑，不是 PPT 技术栈 |
| “多 Agent 协商”真实度 | 3/10 | 协商图存在，但主运行时硬编码关闭；当前更准确叫多角色工作流 |
| 安全与本地降级 | 8/10 | 高风险确认、敏感输入、本地检索与账本是真能力 |
| 48 小时 Demo 潜力 | 8/10 | 只要敢砍范围，两天足够做出可信的垂直切片 |
| 当前发布就绪度 | 4/10 | 可以内部演示，不能诚实地叫完整可发布产品 |

---

## 二、Nielsen 可用性评分：18/40，Poor

| # | 原则 | 分数 | 关键问题 |
| --- | --- | ---: | --- |
| 1 | 系统状态可见性 | 3/4 | 有连接状态、流式阶段、计数和活动状态；保存、失败、同步反馈仍分散 |
| 2 | 系统与现实世界匹配 | 2/4 | 陪伴文案存在，但“衰减图、Header、请求模板、Provider”等实现概念大量泄露 |
| 3 | 用户控制与自由 | 2/4 | 有停止、关闭、刷新；弹层退出、撤销、取消编辑和失败恢复不够统一 |
| 4 | 一致性与标准 | 2/4 | 配色和底栏一致，滚动方式、卡片层级、按钮语义、标题密度不一致 |
| 5 | 错误预防 | 3/4 | 高风险必须确认是真优点；长表单、草稿保护和局部校验没有充分显现 |
| 6 | 识别优于记忆 | 2/4 | 导航有文字，但入口太多、概念重叠，仍要求用户记住模块差异 |
| 7 | 灵活性与效率 | 1/4 | 大量手动刷新，缺少明显快捷键、命令面板、批量处理和设置搜索 |
| 8 | 美观与极简 | 1/4 | 卡片汤、胶囊汤、嵌套边框、工具日志和占位入口同时争抢注意力 |
| 9 | 错误识别与恢复 | 1/4 | 截图几乎没有高质量错误恢复，“刷新”被当成万能药 |
| 10 | 帮助与文档 | 1/4 | 说明很多，但多在解释模块，不是在用户卡住时帮助完成任务 |
| **总分** |  | **18/40** | **需要大幅收敛后再谈完整上线** |

认知负荷 8 项检查中约 6 项失败，属于高负荷。最典型的是记忆区同时给出 8 个顶部入口和 5 个底部主导航，用户一眼面对 13 个导航选择；这不是“功能丰富”，是把产品经理的目录树直接甩到了用户脸上。

---

## 三、逐屏锐评

### 截图 1：首页

这是 22 张图里最像产品的一张，也是最贪心的一张。

优点是角色、夜景、粉色强调和桌面氛围确实有辨识度；问题是日记、目标、宠物状态、记忆时间线、角色标记、聊天框和底栏同时抢主位。角色本应是产品灵魂，现在却像被仪表盘包围的背景图。日期被截成“星期…”，已经直接暴露边界契约不稳。

改法：

- 首页只保留角色、当前状态、一个输入框和最多三条摘要；
- 日记、目标、记忆只显示各一条，不在首页做完整管理；
- 角色脸部和主要动作区域不得覆盖标签；
- “与宠物互动”不应再是额外大按钮，角色舞台和输入框本身就是入口。

### 截图 2：首页确认弹层

这是典型的“为了证明可审计，把审计队列砸到用户脸上”。

弹层巨大、关闭路径不突出、内部出现滚动，背景仍保留大量视觉操作。用户回到桌宠不是来清十条工单的。低风险整理如果默认可撤销，就不该批量塞进确认队列；高风险确认则应一事一确认，说明影响、目标和撤销边界。

改法：

- 首页只露出一条待处理摘要；
- 高风险确认使用单任务侧栏或对话内卡片；
- 低风险动作完成后显示“已整理，可撤销”回执；
- 弹层统一 Portal、焦点陷阱、Esc、明确关闭按钮和单一滚动区。

### 截图 3—5：聊天

用户只说了一个 hello，界面却回以“正在查上下文、正在写回答、来源与整理、工具事件、下次接着聊”。这不是聪明，是系统把后台工作噪声当成了前台价值。

更糟的是，实际回答短，过程卡片比回答大；同一段“下次接着聊”内容重复出现；上方巨大空白、下方信息拥堵；“AI / ME”方块头像又把桌宠人格直接打回通用聊天模板。

改法：

- 第一层只展示用户消息和最终回答；
- 运行中只显示“理解 → 查找 → 执行 → 核验”四类人话阶段；
- 来源、工具、Agent、耗时和节点详情默认折叠到“查看过程”；
- 自动记忆显示一行回执：“已记住，可撤销”；
- 禁止展示原始 prompt、思维链、内部上下文拼接和重复摘要；
- 用角色头像与角色口吻取代“AI”方块；
- 消息正文限制在约 65—75 字符阅读宽度；
- 复杂链若不是真流式，就诚实显示阶段进度，不要把完整答案切块伪装为 token 直播。

### 截图 6：记忆图谱

概念成立，交互不成立。

节点太小、标签缺失、颜色含义弱、连线低对比，右侧详情又太窄。它现在更像“我用了 React Flow”的证明，不像普通人能理解的记忆视图。

改法：

- 图谱降级为“高级分析”，不占一级入口；
- 默认显示重要节点标签；
- 提供搜索、适合视图、图例、筛选和等价列表；
- 不依赖颜色区分类型；
- 100、500、1000 节点分别做性能与可读性降级；
- 明确区分“用户记忆关系图”和“LangGraph 执行图”，二者不是一回事。

### 截图 7—14：记忆工作台

这组页面最能证明信息架构是按开发模块长出来的：

- 档案把查看与新增塞进同一长页；
- 搜索页的搜索框巨大，结果和下一步极弱；
- 数据页是五张同款指标卡加大片空白；
- 导入页是典型三列图标说明模板；
- 关联、热力图直接把“第一阶段保留入口”写给用户看；
- 衰减图标题与实际复核队列不一致；
- 日记报告更像宽灰色文本块，不像能得出行动结论的回顾。

“第一阶段先保留入口”是开发便签，不是产品文案。把半成品直接挂在主导航上，等于主动通知用户：我没做完，但我先占个位。

Demo 中必须把这 9 个入口压成 3 个：

1. **记住的事**：当前记忆、搜索、手动新增；
2. **资料库**：文件、导入、来源；
3. **回顾与维护**：待确认、整理记录、报告、高级分析。

关联、热力图、衰减图如果没有闭环就隐藏；有闭环也只能进入高级分析。

### 截图 15：计划与待办

左表单、中列表、右状态、下步骤、下日志，五个管理区同时出现。你做的是桌宠，不是 Jira 夜间皮肤。

改法：

- 默认只显示“当前任务 + 创建任务”；
- 选中任务后再展开步骤与日志；
- 将创建表单做成独立抽屉或切换面板；
- 任务状态、下一步和截止时间必须比日志更突出；
- 任务动作和 Agent 执行回执合并，避免同一状态重复显示。

### 截图 16—22：设置

这是当前最严重的用户定位错乱。

产品说面向日常中文 Windows 用户，设置页却直接展示 Provider、Base URL、模型名、密钥、鉴权 Header、请求模板、MIME、缓存和内部状态 ID。普通用户不会觉得“专业”，只会觉得自己随时可能配坏。

截图 21 显示文件路径，截图 22 又把 UUID 当作“当前保存位置”，这不是小瑕疵，而是会直接击穿本地存储信任的字段映射问题。

改法：

- 默认设置只留：对话服务、保存位置、自动整理、主动提醒、语音、隐私；
- Provider URL、Header、请求 JSON、按 Agent 分模型全部进入折叠的“开发者设置”；
- 普通模式使用服务预设，只需选服务、填密钥、测试；
- 增加设置搜索或左侧章节导航；
- 全页只保留一个稳定保存栏，不要每段重复“保存/刷新”；
- “保存位置”只能展示经用户理解的本地路径，绝不能展示内部 UUID；
- 任何 credential 只显示是否已配置和掩码，不得回显真实值。

---

## 四、真正值得保留的东西

严厉不等于瞎骂。下面这些是项目真正的资产：

1. **本地优先与可审计不是口号。** SQLite、FTS、Markdown Vault、活动账本、路径安全和敏感输入策略构成了真实差异化。
2. **高风险强制确认是真边界。** 删除、移动、schema、敏感内容等会被判为高风险，设置层也强制确认不可关闭。
3. **LangGraph 和 LangChain 真在主路径。** 当前图会执行、SSE 会发事件、LangChain StructuredTool 也有测试，不是只把依赖写进 pyproject。
4. **检索降级比很多 RAG Demo 扎实。** 专用模型不可用或不调用工具时仍能回落本地 scoped FTS；敏感输入还能绕开远程模型。
5. **桌宠视觉资产有起点。** 夜景、角色和粉色强调足以形成品牌，不需要推倒重画，只需要把后台面板从角色身上搬走。
6. **当前桌面端静态检查通过。** TypeScript typecheck 与运行时乱码校验均通过；问题主要不是“编译不了”，而是产品、布局和真实性。

---

## 五、多 Agent、LangChain、LangGraph：哪些是真的，哪些是说过头了

### 5.1 真的

- apps/backend/app/agents/graph_runtime.py:226-250 使用 StateGraph；
- 当前主图是 route → semantic analysis → retrieval/action/chat → finish；
- graph_runtime.py:60-83 使用 astream(stream_mode="updates")；
- apps/backend/app/services/chat_model.py:258-306 使用 ChatOpenAI 与 LangChain create_agent；
- StructuredTool 有测试覆盖；
- SSE 定义了 status、token、citation、context_budget、agent_action、task、proposal、done、error；
- chat API 负责流式输出、消息落库、失败终态和后台整理；
- reflection 确实存在，但它是聊天完成后的后台 pipeline。

### 5.2 最致命的名不副实：协商图是死代码

仓库里确实有 negotiation_graph.py，也有 orchestrator、invoke-agent、synthesizer 适配器；但当前 GraphRuntime 始终构建普通图。

最关键的证据是：

- graph_runtime.py:391-392 的 _should_use_negotiation() 直接返回 False；
- 应用代码没有 build_negotiation_graph 的真实调用点；
- test_agent_runtime_negotiation.py:91-136 甚至明确断言 use_negotiation=True 也不能进入 orchestrator。

所以设置页里的“多轮结果复核”“复杂请求经过多轮检查和合成”当前属于功能性误导。开关看起来能开，运行时根本不理它。

### 5.3 当前更准确的定义

当前不是多个自治 Agent 并行协商，而是：

> **基于 LangGraph 的多角色 Agent 工作流，支持角色化模型配置、工具调用、本地检索与确定性动作规划。**

当前规范角色是 5 个：

- chat；
- semantic analysis；
- retrieval；
- action；
- reflection。

大量旧 Agent 名称只是别名映射：

- memory/knowledge retrieval → retrieval；
- wiki/memory proposal/task → action；
- diary extractor/continuity → reflection。

action_agent 当前主要还是确定性 Python planner，所有节点共享一个 AgentState，图没有 fan-out、Send 或并行分支。把这叫“多角色工作流”诚实；把它叫“多个自治 Agent 反复协商”就是吹过头。

### 5.4 还缺什么才配叫可靠多 Agent

1. **真实接通协商图**：设置开关必须改变运行图；
2. **有界 supervisor 循环**：最多 2 轮、最多 2 次子 Agent 调用、每次硬超时；
3. **每个角色清晰输入/输出契约**：不要只靠共享大状态；
4. **失败回退**：orchestrator JSON、模型、工具任一失败都回落稳定单图；
5. **节点级 trace**：输入摘要、输出摘要、耗时、工具、token/成本、父子 span；
6. **持久化与恢复**：当前 compile() 没有 checkpointer，POST 后待执行状态还在进程内字典，重启无法恢复；
7. **真正的 HITL 或诚实表述**：现在的人机确认是业务 proposal/API，不是 LangGraph interrupt/resume；
8. **真实复杂路径流式输出**：当前简单聊天是真 streaming，复杂检索路径常是完整结果后再切块；
9. **前台默认隐藏技术细节**：多 Agent 的价值是更稳，不是让用户看日志墙。

注意：checkpointer、持久化 run state 如果涉及 SQLite schema 变化，必须按仓库规则先取得确认、新增 migration、补测试。48 小时 Demo 不建议碰这块。

---

## 六、自动扫描与容器问题：真正的根因不是“再补一条 overflow”

### 6.1 Deterministic detector

对 apps/desktop/src 运行 detector 后只有 1 条 layout-transition warning，定位 desktop-polish.css:14925。它把 SVG 的 stroke-width 误判成盒模型 width 动画，属于正则误报。

结论：

- 原始命中：1；
- 可执行问题：0；
- detector 没抓到真正的布局病灶。

### 6.2 真正的静态病灶

底栏相关变量只在以下媒体查询中定义：

~~~css
@media (max-width: 1366px), (max-height: 780px) {
  --page-padding: ...;
  --bottom-nav-height: ...;
  --bottom-nav-safe-area: ...;
}
~~~

但这些变量在媒体查询之外被大量用于 feature shell、内容 padding、固定导航、聊天、计划、设置和记忆页面。

当窗口同时满足 width > 1366px 且 height > 780px 时，变量可能未定义，依赖它们的整条 CSS 声明会成为无效值。直接后果正是截图里的：

- 固定底栏没有可靠避让区；
- 路由高度计算失效；
- 页面 padding 消失或回退；
- 底部按钮、表单和内容被裁切。

再叠加：

- feature shell 使用 100dvh + overflow:hidden；
- 内层内容再 overflow:auto；
- 底栏 position:fixed、z-index:80；
- Settings 又有外层严格高度 + hidden、内层严格高度 + auto；

任何一个高度变量失效，浏览器不会优雅兜底，只会把内容藏掉。

### 6.3 CSS 已经失控

- desktop-polish.css：394,441 bytes，约 15,508 个物理行；
- dashboard.css：85,353 bytes；
- desktop-polish.css 最后导入，排在 responsive.css、layout-hardening.css、stage-restore.css 后面；
- 文件中出现几十轮“final pass / safety pass / fix / boundary fix”；
- 全样式目录约有 273 条 hidden/clip、71 条 auto/scroll、103 条 fixed/sticky/absolute、117 条 z-index、120 个媒体查询；
- desktop-polish.css 使用至少 24 种数字 z-index。

这不是“CSS 写得多”，而是没有单一布局真相。继续在末尾加补丁，只会让下一张截图修好、上一张截图复发。

### 6.4 必须怎么修

1. 新建唯一的 DesktopShell 布局契约；
2. 全局定义 header、content、dock、安全区变量，不放进条件媒体查询；
3. 每个页面只允许一个主滚动容器；
4. 固定底栏要么进入 grid 第三行，要么统一为 overlay 并由 shell 预留安全区，禁止页面各自猜 padding；
5. 弹层统一 Portal 和层级 token；
6. 建立语义 z-index：content、sticky、dock、backdrop、modal、toast、tooltip；
7. 把 desktop-polish.css 视为迁移素材，不再继续编辑；
8. 按页面迁移到 shell.css、components.css、routes/*.css，迁完一页删一组旧覆盖；
9. 在 1280×720、1366×768、1920×1080 和 Windows 100%/125%/150% 缩放做截图验收；
10. 在 200% 缩放做键盘与可访问性验收。

---

## 七、完整修改建议

## 7.1 产品与信息架构

最终一级导航仍可保留 5 项，但每项必须只有一个清晰任务：

| 一级入口 | 用户来这里做什么 | 默认只展示 |
| --- | --- | --- |
| 首页 | 看见宠物、立即开始 | 角色、状态、一条输入、三条摘要 |
| 对话 | 获得回答并触发动作 | 消息、紧凑阶段、结果回执 |
| 记忆 | 找、看、纠正记忆 | 记住的事 / 资料库 / 回顾维护 |
| 计划 | 创建并推进任务 | 当前任务、创建入口、选中任务详情 |
| 设置 | 完成基础配置 | 对话、保存、自动化、语音、隐私 |

必须移出一级入口：

- 关联；
- 热力图；
- 衰减图；
- 高级 Wiki 管理；
- 每 Agent 模型矩阵；
- TTS 请求模板；
- 图数据库/向量数据库状态；
- 任何写着“第一阶段先保留”的页面。

## 7.2 视觉系统

- 保留夜间观星室和粉色品牌色；
- 取消“万物玻璃卡片化”，只保留背景、内容面、浮层三层；
- 粉色只用于主操作、当前选择和关键状态；
- 普通信息用排版与间距分组，不再套卡；
- 按钮统一 primary、secondary、ghost、danger 四种；
- 状态统一 loading、success、warning、error、disabled；
- 正文至少 14/20，元信息至少 12/16；
- 灰字和半透明背景实测 WCAG AA；
- 移除无意义的胶囊、重复阴影和装饰性 blur；
- 使用角色头像、语气和微交互制造“桌宠感”，不要靠更多面板。

## 7.3 前端工程

- App.tsx 约 58.7 KB、1515 个非空行，应拆为 route shell、providers、chat controller、feature composition；
- MemoryWindowView.tsx 约 104 KB、2632 个非空行，应按三个用户任务拆 container；
- 废止 desktop-polish.css 继续增长；
- 把路由状态、SSE 事件映射、UI 展示状态分层；
- 统一 EmptyState、ErrorState、LoadingSkeleton、ActionReceipt；
- tabs 实现完整 roving tabindex 与方向键；
- 对长文本、emoji、CJK、RTL、1000 条列表做边界测试；
- Electron renderer 继续只走 contextBridge IPC，不引入 Node/FS 直连；
- 敏感 token、Authorization、API key 不进入 renderer 日志。

性能方面也不能再靠“数据现在不多”掩盖：

- App.tsx:1146 每次渲染都会重建 agentActivityEntries，应按依赖 memoize，并避免把无关状态全部挂在顶层；
- ChatMessageList.tsx:60 与 MemoryWindowView.tsx:2607 仍对完整可见列表直接 map，超过阈值后应虚拟化或分页；
- BottomNav.tsx:101、App.tsx:1097 等导航过程会触发 smooth scroll；应尊重 reduced motion，并避免导航时同步布局读写；
- useDesktopWindowRouting.ts:30 会改 document.body.dataset.windowMode，而 desktop-polish.css 有大量 data-window-mode 选择器，路由切换可能触发广泛样式重算；
- 用 React Profiler 和 Chromium Performance 对 route switch、长聊天、100/1000 条记忆做基线，不要只测空状态截图。

## 7.4 聊天与 Agent UX

用户默认只看：

~~~text
用户问题
→ 一行阶段状态
→ 最终回答
→ 来源
→ 动作回执（已创建任务 / 已记住 / 需要确认 / 可撤销）
~~~

展开“查看过程”后才看：

~~~text
语义分析  120 ms
检索记忆  3 条来源  680 ms
复核证据  通过
生成回答  1.2 s
~~~

永远不要展示：

- 原始 system prompt；
- 原始模型推理；
- credential；
- 大段内部上下文；
- 重复的“下次接着聊”总结；
- 把完整回答切块后伪称真实 token streaming。

## 7.5 后端多 Agent

短期：

- 真正接通 negotiation graph；
- 固定两轮；
- supervisor 只在检索证据不足时追加一次 retrieval；
- action 继续确定性执行，不必为了“更 Agent”而让模型直接写文件；
- 复用现有 SSE negotiation_step / negotiation_done；
- 任一异常回退普通图；
- 模型角色先共用同一 provider 和模型，仅用 prompt 区分。

中期：

- 增加 LangGraph checkpointer 与可恢复 thread；
- 将 run state 持久化；
- 为节点、模型和工具增加 span；
- 实现清晰的 approval interrupt/resume；
- 将 reflection 明确建模为后台图或任务队列；
- 对 supervisor 循环、超时、重复调用和预算做硬上限；
- 再考虑真正并行 fan-out，而不是为了炫技先上并发。

## 7.6 记忆与资料

- SQLite、FTS、Markdown 继续作为用户可见真相；
- Qdrant/Kuzu 保持可选镜像，不做 Demo 必需依赖；
- 搜索必须返回来源、片段和空证据结论；
- 记忆写入显示原因、来源、风险、目标、状态和撤销；
- 图谱只做辅助浏览，不取代列表；
- “档案、日记、资料、记忆”统一术语，不让用户猜边界；
- 保存位置显示真实可理解路径，不显示内部 ID。

## 7.7 质量、可访问性与安全

- 所有异步操作有 loading、success、error、retry；
- 保存失败不得清空表单；
- 断线后不能同时出现 done 与 error；
- 空检索必须拒绝无来源的事实回答；
- 高风险动作只生成确认请求，不先改目标；
- 键盘可完成连接、聊天、查看来源、确认/取消、创建任务；
- reduced motion 必须覆盖所有动画；
- 图谱必须有等价列表；
- 任何打包物不得带 SQLite、db、credential；
- live provider gate 未跑就不能把供应商可用性写成 Covered。

---

## 八、如果只有 2 天，如何上线一个“完整版感觉”的同功能 Demo

先接受一个残酷事实：

**两天做不出 22 张页面的完整版，但完全能做出一个让人相信核心能力已经成立的完整垂直切片。**

Demo 的“完整”不是页面数量，而是从输入到结果、记录、确认、撤销和再次检索都能闭环。

### 8.1 一句话 Demo 定位

> Agent Pet 是一个本地优先的 Windows AI 伴侣：它会通过 LangGraph 多角色工作流理解问题、检索本地记忆、给出带来源回答，并把任务或记忆以可追踪、可确认、可撤销的方式保存下来。

### 8.2 只保留这些

| 保留 | 原因 |
| --- | --- |
| 首页舞台 | 品牌与第一印象 |
| 对话 | 核心入口 |
| 一条真实 LangGraph 主链 | 技术主叙事 |
| SQLite FTS + Vault 检索 + citation | 本地记忆价值 |
| SSE 状态、来源、done/error | 可观测体验 |
| 任务创建 | 证明 Agent 不只会聊天 |
| 高风险确认 | 证明安全边界 |
| 活动账本与撤销回执 | 证明可审计 |
| 一个全局模型配置与连接测试 | 保证可运行 |
| 基础保存位置与隐私设置 | 保证本地优先叙事 |

### 8.3 直接隐藏这些

- 关联、热力图、衰减图；
- 高级 Wiki ingest/lint/repair；
- 每 Agent 独立模型复杂 UI；
- Qdrant/Kuzu 强依赖；
- TTS Header/JSON/MIME/缓存管理；
- 可编辑 2—10 轮协商；
- “可恢复长任务、并行 Agent、自动自我修复”等尚未成立的宣传；
- 所有占位页；
- 所有没有现场演示脚本的按钮。

### 8.4 两条黄金路径

#### 黄金路径 A：带来源的记忆回答

演示输入示例：

> 我之前提过更喜欢上午还是下午开会？顺便告诉我这个结论来自哪条记录。

目标链：

~~~text
START
→ semantic_agent
→ supervisor
→ retrieval_agent（SQLite FTS / Vault）
→ supervisor/reviewer（证据不足时最多再检索一次）
→ chat_agent 合成带引用回答
→ finish
~~~

前台只显示：

~~~text
理解问题 → 查找本地记忆 → 核验来源 → 完成
~~~

展开后才显示 Agent、工具、耗时与来源。

#### 黄金路径 B：从对话创建任务

演示输入示例：

> 明天下午三点提醒我给张老师回邮件，并记住我更喜欢下午开会。

目标结果：

- semantic 判定 reminder + memory；
- action 生成确定性计划；
- 低风险任务创建成功；
- 记忆按当前策略写入或生成确认；
- 聊天显示两个紧凑回执；
- 活动账本可查看；
- 若把动作改成删除/移动/批量重写，系统必须只请求确认，不执行。

### 8.5 48 小时排期

以下按一名熟悉仓库的开发者、两个正常工作日安排，不是假设 48 小时不睡觉。

#### 第一天上午：冻结范围与修真话

1. 隐藏所有占位 Tab；
2. 隐藏或改写虚假的“多轮复核”设置；
3. 确定只演示两条黄金路径；
4. 固定 Demo 数据与脚本；
5. 给全局布局变量补默认值；
6. 建立唯一 shell 滚动与 dock 安全区。

交付标准：

- 1366×768 下首页、聊天、记忆、计划、设置无底栏遮挡；
- 页面无开发阶段文案；
- 用户五秒内知道从哪里开始。

#### 第一天下午：接通有界协商图

1. use_negotiation=True 时真正构建 negotiation graph；
2. 最多 2 轮、最多 2 次子 Agent 调用；
3. 每次模型/工具调用有 timeout；
4. orchestrator 解析失败立即回退稳定普通图；
5. 改写当前“协商开关被忽略”的测试；
6. 验证 negotiation_step 与 negotiation_done；
7. 打通检索、citation、空证据拒答。

交付标准：

- 开关开启时确实出现协商事件；
- 关闭时普通图不回归；
- 不会无限循环；
- 模型失败仍有稳定错误或本地降级。

#### 第一天晚上：前台把技术藏对

1. 聊天默认只显示回答；
2. 过程压缩为 3—4 个阶段；
3. 来源和 Agent trace 默认折叠；
4. 删除重复“下次接着聊”和大块工具内容；
5. 统一结果回执。

#### 第二天上午：任务、安全与演示数据

1. 打通任务创建；
2. 验证低风险自动执行；
3. 验证高风险只请求确认；
4. 准备隔离 Demo Vault，不使用真实用户 Vault；
5. 预置 3 条日记、5 条长期记忆、1 份项目资料和 2 个任务；
6. 准备 provider 不可用时的可解释失败，不伪造成功。

#### 第二天下午：验收、打包、彩排

1. 跑后端全量测试；
2. 跑桌面 typecheck、关键 Vitest；
3. 跑 Electron migration 与 package check；
4. 跑气泡、资源、打包验证；
5. 用真实 provider 连跑至少 5 次固定脚本；
6. 1280×720、1366×768、1920×1080，100%/125%/150% 缩放截图；
7. 彩排 5—7 分钟演示；
8. 最后 2—3 小时只修 P0，不再加功能。

### 8.6 Demo 必须通过的 10 个场景

1. 普通聊天：非空 token + done；
2. 记忆问题：semantic → retrieval → citation → synthesis；
3. 空检索：明确无本地证据，不编造；
4. 协商开启：至少一个 negotiation_step 和最终 negotiation_done；
5. 最大两轮：低置信度或循环会 fallback；
6. 创建任务：产生 task event，数据库可查询；
7. 高风险动作：requires_confirmation=true，目标不被修改；
8. 敏感输入：不调用远程模型，仅走本地隐私路径；
9. provider 超时或鉴权失败：稳定 error，不能同时 done；
10. SSE 断流：agent_run/message 最终状态为 failed。

### 8.7 5—7 分钟演示脚本

1. 30 秒：打开首页，说明本地优先与桌宠定位；
2. 90 秒：询问一条已存在偏好，展示带来源回答；
3. 30 秒：展开 Agent 过程，证明 semantic/retrieval/reviewer/chat 的有界协作；
4. 90 秒：一句话创建任务并记住偏好；
5. 45 秒：打开活动账本，展示目标、来源、风险和撤销；
6. 45 秒：演示一个高风险动作被确认门拦住；
7. 30 秒：打开基础设置，展示保存位置和本地隐私；
8. 30 秒：回到首页收尾，不要现场探索那些没彩排的页面。

### 8.8 时间不够时的砍功能顺序

按这个顺序砍，绝对不要反过来：

1. 先砍图谱动画和高级可视化；
2. 再砍 TTS 高级能力；
3. 再砍每 Agent 独立模型配置；
4. 再砍 Wiki 高级管理；
5. 再砍第二条黄金路径；
6. 最后才允许砍真实协商链。

如果协商链接不稳，就退回当前稳定 LangGraph 主图，并把文案诚实改成“多角色工作流”。**宁可少一个卖点，也不要演示一个开关看起来打开、运行时实际无效的功能。**

---

## 九、上线后的完整路线

### 第 1 周：把产品从“能看”修成“能用”

- 重做 shell、scroll、dock、modal；
- 记忆导航 9 合 3；
- 设置拆普通/开发者；
- 聊天结果优先；
- 清理占位页；
- 建立视觉 token 和组件状态；
- 开始拆 desktop-polish.css。

### 第 2—3 周：让多 Agent 配得上宣传

- 接通有界 negotiation；
- 节点级 trace；
- token/耗时/工具统计；
- supervisor fallback；
- reflection 后台图；
- 完整 Agent 契约测试；
- 真实 provider 回归。

### 第 4—6 周：可靠性与恢复

- 经确认后新增 migration；
- LangGraph checkpointer；
- durable run state；
- interrupt/resume；
- 断线续跑；
- crash/restart 恢复；
- 审批队列与撤销一致性。

### 第 6 周以后：再谈高级可视化

只有当普通用户已经稳定完成“聊天、找记忆、建任务、确认、撤销”后，才重新开放：

- 图谱；
- 关联；
- 热力图；
- 衰减；
- 多模型矩阵；
- 高级 Wiki 工作流。

否则这些都只是技术陈列品。

---

## 十、最终判词

这个项目最大的优点是：**它不是空壳。**

这个项目最大的缺点也是：**你太急着证明它不是空壳，于是把所有内部结构都掏出来摆在用户面前。**

后端复杂，前台就应该更简单；Agent 越多，用户越不应该看到日志墙；本地记忆越强，首页越应该像陪伴，而不是数据库驾驶舱。

当前最该做的不是继续添加第十个记忆 Tab，也不是继续在 desktop-polish.css 末尾写第 N 个 final pass。你应该：

1. 砍掉半数入口；
2. 修掉单一布局契约；
3. 接通一条真的协商链；
4. 把技术过程折叠；
5. 用两条黄金路径证明价值。

做到这五件事，两天后你能拿出一个“功能不多但完整、技术真实、演示稳定”的 Demo。

继续现在的路线，两天后你只会得到更多页面、更多 CSS、更多按钮，以及更多需要解释为什么被遮住的截图。

---

## 十一、本次证据与命令记录

说明：以下记录本次主 Agent 与并行审计中所有具有判定作用的命令；纯粹为分段读取同一长文件的命令合并记录。所有操作均为只读，除最终新增本文件外未修改源码、未修改 progress.md、未触碰生成物和本地数据库。

### 11.1 工作区与文件检查

~~~text
命令：
rg --files -g AGENTS.md -g '!apps/desktop/node_modules/**' -g '!apps/desktop/dist/**' -g '!apps/desktop/release/**'

结果：exit 0
vault\Wiki\AGENTS.md
AGENTS.md
~~~

~~~text
命令：
git rev-parse --is-inside-work-tree
git status --short

结果：exit 0
当前是 Git worktree；存在大量用户已有的 modified/deleted/untracked 项，包括 backend chat、desktop App/Memory/Settings、desktop-polish.css 及既有 Playwright 输出。本次未覆盖或回滚这些改动。
另有 Git 全局 ignore 权限 warning，不影响状态输出。
~~~

~~~text
命令：
Test-Path .\锐评.md
Test-Path apps\backend\app\agents\graph_runtime.py
Test-Path apps\desktop\electron\main.cjs
Test-Path docs\current-specification.md

结果：
False
True
True
True
~~~

### 11.2 Impeccable / Playwright 上下文

~~~text
命令：
Get-Content -Raw "$env:CODEX_HOME\skills\impeccable\SKILL.md"
node "$env:CODEX_HOME\skills\impeccable\scripts\context.mjs" --target apps\desktop
Get-Content -Raw "$env:CODEX_HOME\skills\impeccable\reference\critique.md"
Get-Content -Raw "$env:CODEX_HOME\skills\impeccable\reference\product.md"

结果：
均完成读取；context exit 0，识别 PRODUCT.md，register=product，targetExists=true，designPath=null。
~~~

~~~text
命令：
Get-Content -Raw "$env:CODEX_HOME\skills\playwright\SKILL.md"
Get-Command npx
Get-Command playwright-cli
检查 5173/5174 listener

结果：
npx=C:\Program Files\nodejs\npx.ps1
playwright-cli 未全局安装
5173 NOT_LISTENING
5174 NOT_LISTENING
未启动或清理进程；因此没有把本次截图审计冒充为新鲜浏览器自动化通过。
~~~

### 11.3 前端静态证据

~~~text
命令：
统计 App.tsx、MemoryWindowView.tsx、dashboard.css、desktop-polish.css、layout-hardening.css、responsive.css 的 bytes / lines / !important

结果：exit 0
App.tsx                    BYTES=58765   LINES(non-empty)=1515
MemoryWindowView.tsx       BYTES=103980  LINES(non-empty)=2632
dashboard.css              BYTES=85353   LINES(non-empty)=3729   IMPORTANT=3
desktop-polish.css         BYTES=394441  LINES(non-empty)=13347  IMPORTANT=9
layout-hardening.css       BYTES=5694    LINES(non-empty)=246    IMPORTANT=9
responsive.css             BYTES=9624    LINES(non-empty)=429    IMPORTANT=7

补充：(Get-Content desktop-polish.css).Count = 15508 个物理行。
~~~

~~~text
命令：
Select-String desktop-polish.css -Pattern '^/\*'

结果：exit 0
发现从 Product Design pass 到多轮 final/safety/boundary/revert/polish pass 的大量补丁段，最后一组位于 15k 行附近。
~~~

~~~text
命令：
node "$env:CODEX_HOME\skills\impeccable\scripts\detect.mjs" --json apps/desktop/src

结果：exit 1（规则命中，不是脚本崩溃）
total=1
severity=warning
rule=layout-transition
file=apps/desktop/src/styles/desktop-polish.css
line=14925

复核：命中来自 stroke-width，被 detector 的 width 正则误报。
可执行问题=0。
~~~

~~~text
命令：
统计 styles/*.css 中 overflow / position / z-index / @media

结果：exit 0
overflow hidden/clip = 273
overflow auto/scroll = 71
position fixed/sticky/absolute = 103
z-index = 117
@media = 120
~~~

~~~text
命令：
rg -- '--page-padding|--bottom-nav-height|--bottom-nav-safe-area' apps/desktop/src

结果：exit 0
确认变量只在 max-width:1366 或 max-height:780 的条件媒体查询内定义，却在条件外被多个路由使用。
~~~

### 11.4 桌面端验证

~~~text
命令：
Push-Location apps\desktop; npm run typecheck; Pop-Location

结果：exit 0
> agent-pet-desktop@0.0.1-alpha typecheck
> tsc --noEmit
~~~

~~~text
命令：
Push-Location apps\desktop; npm run runtime:mojibake:check; Pop-Location

结果：exit 0
> agent-pet-desktop@0.0.1-alpha runtime:mojibake:check
> node scripts/validate-runtime-mojibake.mjs

Runtime mojibake validation passed.
~~~

### 11.5 Agent / LangGraph 静态核查

~~~text
命令：
rg / Get-Content 定位 StateGraph、astream、agent registry、negotiation graph、orchestrator、checkpointer、interrupt、SSE events、settings、migrations 与测试

结果：
- 当前主图与 astream 已接通；
- negotiation graph 存在；
- _should_use_negotiation() 返回 False；
- 没有 checkpointer、SqliteSaver、MemorySaver、interrupt、Command 或 thread_id 命中；
- 5 个规范 Agent 角色存在；
- legacy Agent ID 会映射到 retrieval/action/reflection。
~~~

### 11.6 后端聚焦测试

~~~text
命令：
Push-Location apps\backend
python -m pytest -q tests/test_agent_runtime.py tests/test_agent_runtime_chat.py tests/test_agent_runtime_routing.py tests/test_agent_runtime_retrieval.py tests/test_agent_runtime_memory_task.py tests/test_agent_runtime_wiki.py tests/test_agent_runtime_negotiation.py tests/unit/agents/test_registry.py tests/unit/agents/test_agent_runner.py tests/unit/agents/nodes/test_orchestrator.py tests/unit/agents/test_tool_timeouts.py
Pop-Location

结果：exit 0
........................................................................ [100%]
72 passed in 1.50s
~~~

~~~text
命令：
Push-Location apps\backend
python -m pytest -q tests/test_e2e_backend_mvp.py::test_chat_returns_stream_url_and_stream_emits_sse_events tests/test_api_wiring_mvp.py::test_tasks_and_chat_sse_are_wired tests/test_api_wiring_mvp.py::test_chat_stream_fails_when_runtime_ends_without_terminal_event tests/test_api_wiring_mvp.py::test_chat_run_lookup_removes_expired_unstreamed_runs tests/test_api_wiring_mvp.py::test_retrieval_chat_stream_emits_citation_event tests/test_api_wiring_mvp.py::test_plain_chat_stream_answers_without_citation tests/test_api_wiring_mvp.py::test_task_chat_stream_emits_task_event tests/test_api_wiring_mvp.py::test_sensitive_memory_chat_stream_rejects_without_proposal
Pop-Location

结果：exit 0
........                                                                 [100%]
8 passed in 6.83s
~~~

这些测试证明当前普通图、工具、检索、SSE 与安全降级扎实；其中 negotiation 测试证明的是“当前刻意不启用协商”，不能用 72 个通过项宣称多 Agent 协商已经上线。

### 11.7 本次未运行

- 未运行 live provider gate；
- 未启动 Electron 或 renderer；
- 未运行全量 backend pytest；
- 未运行 npm build/package；
- 未修改任何 schema、migration、Vault 或本地数据库；
- 未把任何未验证项写成 Covered。

### 11.8 本文档落盘后的验证

~~~text
命令：
Get-Item .\锐评.md
(Get-Content -Encoding utf8 .\锐评.md).Count
Select-String 检查关键章节、18/40、_should_use_negotiation 与 48 小时标题

结果：exit 0
FullName=E:\agentproject\锐评.md
关键章节与关键结论均命中；UTF-8 中文读取正常。
~~~

~~~text
命令：
.\scripts\check-mvp-acceptance-gap.ps1

结果：exit 1
note: Checked MVP matrix, persistent reminder runtime, Live2D partial coverage, tray integration, and updater gap.
MVP acceptance gap contract failed:
- Coverage matrix does not mark 'Full Live2D' as 'Partial / Gap'.
- Coverage matrix is missing gap detail: lacks lip sync, complex motion sequencing, multi-character resource management

判断：这是现有 docs/mvp-acceptance-coverage.md 与脚本契约之间的缺口，与新增锐评.md无直接关系。本次没有擅自把任何状态改成 Covered，也没有修改 coordinator-owned progress.md。
~~~

### 11.9 前端性能热点复核

~~~text
命令：
rg -n "buildAgentActivityEntries|document\.body\.dataset\.windowMode|scrollIntoView\(|filteredEntries\.map|visibleMessages\.map" apps\desktop\src

结果：
App.tsx:1146 重建 agentActivityEntries
App.tsx:1097 使用 smooth scroll
BottomNav.tsx:101 使用 smooth scroll
ChatMessageList.tsx:60 对 visibleMessages.map
MemoryWindowView.tsx:2607 对 filteredEntries.map
useDesktopWindowRouting.ts:30 修改 body.dataset.windowMode
desktop-polish.css 存在大量 body[data-window-mode=...] 规则
~~~
