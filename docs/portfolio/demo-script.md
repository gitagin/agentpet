# Narrowed Demo Script

Use only isolated `.tmp` data. Do not bind a real Vault or configure a live
provider for this accepted script.

1. State that the demo uses SQLite, Markdown, and FTS-first retrieval.
2. Ask for the seeded meeting-time preference and show the accepted citation.
3. Create the seeded reminder/memory compound action and inspect receipts.
4. Show a high-risk destructive request stopping at confirmation.
5. Reject one decision and show zero effect; approve the isolated checkpoint
   path and show exactly-once behavior.
6. Enable the local privacy scenario and show that the remote-call count stays
   zero for policy-detected sensitive input.
7. Open the activity ledger and show one reversible isolated action.
8. Close with the limitations: FTS is default, hybrid/reranker remain Defer,
   and no live-provider or packaged-executable claim is made.

Accepted evidence: TASK-1212 through TASK-1216 verification records. Do not
substitute screenshots or seeded DOM for API-backed citations and receipts.
