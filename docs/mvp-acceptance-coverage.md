# MVP Acceptance Coverage Matrix

Date: 2026-05-02
Executor: Codex, Backend Core Worker, Desktop Workflow Worker
Last reconciled: 2026-06-07 by Codex against current implementation files, private desktop-pet auto-organization, post-chat Wiki distillation guardrails, and dual-track memory regression evidence.

This matrix records the current MVP acceptance boundary against the implementation. Superseded specification snapshots were removed during documentation consolidation; current code and rerun evidence take precedence.

Status legend:

- `Covered`: current implementation has automated coverage or a direct implementation signal.
- `Partial`: core path exists, but one or more acceptance details remain unverified or incomplete.
- `Gap`: acceptance outcome is not implemented.
- `Out of scope`: the item belongs to Beta or formal release acceptance rather than MVP.

## Compact Evidence Ledger

| Task | Status | Preserved boundary |
| --- | --- | --- |
| TASK-0101 | Completed by user manual verification | On 2026-06-19 the user confirmed that double-clicking the Pet window opened the main stage; recording was explicitly waived, so no replayable media exists. |
| TASK-0102 | Skipped / not completed | The isolated-or-backed-up Vault chain Chat → archive → memory view → explicit revert → database/file before-and-after evidence was not run. |
| TASK-0202 | Implemented, manual gate skipped | Markdown/JSON memory export and second-confirmation revert have automated coverage, but no human download/revert observation exists. |
| TASK-0502 | Completed at L3 for the tested detector path | A fake model endpoint received 0 requests for a policy-detected sensitive message; this is detector-based local privacy, not universal network isolation. |
| TASK-0503 | In Progress, L4 pending | Completion requires at least 20 real dialogue samples, zero raw path/id/score/secret leakage, at least 80% natural or acceptable labels after revision, a follow-up for every unsafe/over-recall sample, and human sign-off. |
| TASK-0504 | Completed mechanism path at L3 | Real chat activity can advance the four growth dimensions; no seven-day retention evidence exists. |
| TASK-0505 | Completed mechanism path at L3 | Frequency, cooldown, quiet/busy guards and value-based candidates are implemented; no seven-day retention evidence exists. |

## MVP Matrix

| ID | Scenario | Expected result | Current status | Evidence | Gap / next check |
| --- | --- | --- | --- | --- | --- |
| MVP-01 | First launch | UI appears within 10 seconds and sidecar health is ok | Covered | `apps/desktop/electron/main.cjs` starts the FastAPI sidecar and waits on `/api/health`; the managed sidecar now defaults backend state to `app.getPath("userData")\backend-state` via `AGENT_PET_DATA_DIR` unless an explicit state override is present; `apps/backend/tests/test_api_wiring_mvp.py::test_vault_status_recovers_persisted_active_vault_after_restart` verifies active Vault recovery from persisted backend state; `apps/desktop/package.json` exposes Electron packaging scripts; `docs/runbook.md` documents local launch; user-confirmed manual Electron validation on 2026-05-15 covered startup timing, health, control-console access, and active Vault restore. | None for the v0.2 local launch boundary; signed installer and auto-update remain out of scope. |
| MVP-02 | Auth | Calling `/api/vaults/status` without token returns 401 | Covered | `apps/backend/tests/test_e2e_backend_mvp.py::test_protected_mvp_apis_require_authorization`; `apps/backend/tests/test_security_hardening_mvp.py`. | None for current API contract. |
| MVP-03 | Health check | `/api/health` works without token and does not expose Vault/model state | Covered | `apps/backend/tests/test_e2e_backend_mvp.py::test_health_has_no_auth_requirement_and_no_sensitive_state`; `apps/backend/app/models/api.py::HealthResponse`. | None for current API contract. |
| MVP-04 | Vault init | Empty directory creates default folders/templates | Covered | `apps/backend/app/api/vaults.py::init_vault`; `apps/backend/tests/test_e2e_backend_mvp.py::test_vault_init_creates_and_binds_temp_vault`. | None for current backend contract. |
| MVP-05 | Path safety | Vault-outside file access returns 403 and writes audit log | Partial | Path traversal and unsafe target paths are rejected in `apps/backend/tests/test_storage_paths.py` and `apps/backend/tests/test_security_hardening_mvp.py`; `apps/backend/app/api/vaults.py` records `vault.bind` denied/failed audit rows and `apps/backend/tests/test_audit_logs.py::test_memory_and_denied_vault_paths_are_audited` covers denied Vault bind auditing. | Audit coverage exists for denied Vault bind and write paths, but this row stays Partial until all unsafe file-access cases are normalized to the expected 403 contract and each security-path denial has direct audit assertions. |
| MVP-06 | Index | A Vault with Markdown files populates `notes`, `note_chunks`, and `note_fts` | Covered | `apps/backend/app/repositories/storage.py::replace_note`; `apps/backend/tests/test_retrieval_fts.py`; `apps/backend/tests/test_api_wiring_mvp.py`. | Existing tests use focused fixtures, not exactly 20 files. |
| MVP-07 | Retrieval | Searching an existing keyword returns `relative_path` and `snippet` | Covered | `apps/backend/tests/test_e2e_backend_mvp.py::test_markdown_index_then_memory_search_returns_citation`; `apps/backend/app/models/api.py::MemorySearchResult`. | None for FTS mode. |
| MVP-08 | Chat | Asking about existing Vault info returns SSE `token`, `citation`, and `done` | Covered | `apps/backend/app/api/chat.py`; `apps/backend/app/agents/graph_runtime.py` runs the core route/chat/search/memory/task flow through LangGraph nodes, maps model-triggered LangChain `StructuredTool` results back to SSE events, falls back to grounded Vault search when a real model skips `search_memory`, and refuses model-only grounded answers when search returns no citation; `apps/backend/app/repositories/storage.py` normalizes natural English/Chinese questions into significant retrieval terms and preference/task/reminder synonyms; `apps/backend/app/services/chat_model.py` wraps LangChain `create_agent` + `ChatOpenAI`; `apps/backend/tests/test_agent_runtime.py` validates fake model-driven search/memory/task events, no-tool grounded fallback, and empty-search non-hallucination; `apps/backend/tests/test_retrieval_fts.py` validates natural English/Chinese retrieval questions; `apps/backend/tests/test_live_model_chat_acceptance.py` adds the live-provider gate for saved settings, `/api/settings/model-test`, `/api/chat`, `citation`, non-empty `token`, and final `done`; user confirmed the real provider live gate passed on 2026-05-15. | None for current v0.2 chat/live-provider acceptance; provider credentials must still stay out of logs and diagnostics. |
| MVP-09 | Stream auth | Streaming endpoint without token returns 401 and emits no events | Covered | `apps/backend/tests/test_e2e_backend_mvp.py::test_protected_mvp_apis_require_authorization`; renderer `apps/desktop/src/services/sse.ts` does not own bearer material, while `apps/desktop/electron/proxy.js` injects authorization inside the Electron boundary and its proxy tests lock that contract. | None for current API contract. |
| MVP-10 | Candidate memory | Input "remember..." is captured without silently promoting sensitive content | Covered | Default private-assistant mode no longer shows ordinary memory as a pending proposal: `apps/backend/app/agents/graph_runtime.py` and `apps/backend/app/agents/nodes/memory.py` defer safe explicit memory requests to the background long-term-memory archive, while sensitive content is rejected and `auto_long_term_memory=False` preserves the old confirmation item path; `apps/backend/tests/test_agent_runtime.py`; `apps/backend/tests/test_agent_actions.py`; `apps/backend/tests/test_memory_services.py`. | The original proposal-first UX remains available for disabled-auto/high-risk flows; ordinary high-confidence memory should appear as recent automatic activity, not as a manual review card. |
| MVP-11 | Confirm write | High-risk or disabled-auto confirmation updates Markdown, marks confirmed, and refreshes index | Covered | `apps/backend/app/services/memory.py::confirm_proposal`; `apps/backend/app/api/wiring.py` passes `index_refresh`; `apps/backend/tests/test_memory_services.py`; `apps/backend/tests/test_e2e_backend_mvp.py`; `apps/backend/tests/test_audit_logs.py` covers memory proposal create/confirm/reject audit rows; `apps/backend/tests/test_api_wiring_mvp.py::test_chat_stream_auto_archives_daily_memory_and_records_action` covers the ordinary automatic activity path. | None for current backend contract. |
| MVP-12 | Write conflict | Manual target edit after proposal creation returns 409 and requires diff regeneration | Covered | `apps/backend/tests/test_memory_services.py::test_confirm_rejects_when_target_content_hash_changed`; API error mapping in `apps/backend/app/api/wiring.py`. | Diff-regeneration UX is not separately automated. |
| MVP-13 | Reject write | Rejecting a proposal marks rejected and leaves Markdown unchanged | Covered | `apps/backend/tests/test_memory_services.py::test_reject_proposal_does_not_write_markdown`; `apps/backend/tests/test_e2e_backend_mvp.py`. | None for current backend contract. |
| MVP-14 | Reminder | Reminder due time triggers system notification and marks reminder triggered | Covered | `apps/backend/app/scheduler/reminders.py` uses `APSchedulerReminderScheduler` with `AsyncIOScheduler` and SQLite `SQLAlchemyJobStore`; persisted jobs call `fire_reminder_job`, which marks reminders `triggered`, records `triggered_at`, and writes scheduler audit rows; `apps/backend/tests/test_reminder_scheduler.py` covers restart persistence, due-time trigger delivery, and cancelled job non-restoration; `apps/backend/tests/test_tasks_services.py::test_scheduler_trigger_marks_reminder_triggered` verifies the service transition contract; `apps/desktop/electron/main.cjs` exposes `agent-pet:show-reminder-notification` and uses Electron `Notification`; `apps/desktop/src/App.tsx` polls task state and sends one desktop notification per triggered reminder; Manual Electron validation on 2026-05-15 confirmed near-term reminder triggered and emitted one OS notification. | None for current v0.2 reminder delivery; OS permission-denial behavior remains tracked under MVP-16. |
| MVP-15 | Time parsing | Natural-language "tomorrow afternoon at 3" uses frontend timezone, stores UTC, and returns timezone | Partial | `apps/backend/app/services/tasks.py` parses common Chinese reminder expressions such as today/tomorrow/day-after-tomorrow, morning/afternoon/evening, `X点`, `X点半`, `X:MM`, `X秒/秒钟后`, `X分钟后`, and `X小时后`; `apps/backend/tests/test_tasks_services.py` and `apps/backend/tests/test_e2e_backend_mvp.py` verify natural-language reminder creation, explicit local datetime plus timezone conversion, Beijing timezone aliases, customer-facing `timezone_label`, UTC storage, and short-delay reminder parsing. | Full natural-language date parsing is not implemented; current coverage is intentionally limited to common Chinese reminder expressions. |
| MVP-16 | Reminder scheduling failure | Disabled notification permission keeps task and marks reminder unscheduled | Partial | Scheduler failure path is covered in `apps/backend/tests/test_tasks_services.py::test_scheduler_failure_keeps_task_and_marks_reminder_unscheduled`; Electron denies renderer permission requests in `apps/desktop/electron/main.cjs`. | Failure is simulated by an in-memory scheduler flag, not by OS notification permission denial. |
| MVP-17 | Sensitive information | API-key-like memory is rejected or requires explicit secure confirmation, and does not write Vault | Covered | `apps/backend/app/services/memory_policy.py`; `apps/backend/tests/test_security_hardening_mvp.py`; `apps/backend/tests/test_agent_runtime.py`. | None for current rejection path. |
| MVP-18 | Logs | Chat and write logs include `request_id` and `agent_run_id` | Covered | `apps/backend/app/services/audit.py` writes structured audit/application logging implementation rows; `apps/backend/app/api/chat.py`, `apps/backend/app/api/memory.py`, `apps/backend/app/api/vaults.py`, `apps/backend/app/api/tasks.py`, and `apps/backend/app/main.py` now record chat, write, Vault, task, and reminder events with `request_id`/`agent_run_id` context; `apps/backend/tests/test_audit_logs.py` covers the log contract and diagnostics redaction. | Remaining diagnostics output stays redacted by design; no separate file-based audit sink is implemented. |

## Non-MVP Acceptance Notes

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Sprite-pet portrait runtime | Covered | `apps/desktop/src/views/StageView.tsx` and `apps/desktop/src/features/desktop/ControlDashboard.tsx` render the home character through `HalfbodyPetPortrait`; `apps/desktop/public/sprite-pet/state.json` describes the home-only layered portrait; `apps/desktop/scripts/validate-sprite-pet-assets.mjs` checks base, blink, and viseme PNG resources; production Live2D fallback, Cubism runtime aliases, and `public/live2d` resources have been removed. | Covered for the home layered-portrait scope. Q-version sprite sheet pet remains future pet-window work and should stay separate from the home halfbody portrait. |
| Full Live2D | Partial / Gap | The current product has a layered halfbody portrait and sprite-pet asset checks, not a complete Live2D production runtime. | Full Live2D lacks lip sync, complex motion sequencing, multi-character resource management. |
| Actual signed auto-update | Out of scope / Gap | `apps/desktop/package.json` uses `electron-builder` but has no `publish`, signing, or `autoUpdater` contract. | Current packaging is a Windows trial dir/zip path, not a signed update channel. |
| APScheduler-backed reminder runtime | Covered | `apps/backend/app/scheduler/reminders.py` implements `APSchedulerReminderScheduler` with SQLite `SQLAlchemyJobStore`; `apps/backend/app/main.py` starts it paused, recovers reminders, then resumes it; `apps/backend/tests/test_reminder_scheduler.py` covers persistence, delivery, and cancellation. | Persistent scheduler runtime is now part of the v0.2 reminder contract; desktop notification permission behavior remains tracked under MVP-16. |
| Tray integration | Partial | `apps/desktop/electron/main.cjs` creates a `Tray` with desktop-pet, control-console, and quit entries when an icon is available. | Tray presence is implemented for local trial convenience; OS-specific tray behavior and notification delivery remain outside backend smoke scope. |
| Desktop-pet compact reply bubble | Covered | `apps/desktop/src/App.tsx` stores the full streamed Agent reply while `apps/desktop/src/services/petBubblePagination.ts` generates compact semantic pages; `apps/desktop/pet-hitbox.json` keeps `chatBubble` at 214x58; `apps/desktop/scripts/validate-pet-bubble-pagination.mjs` validates pagination boundaries and grapheme preservation; user-confirmed manual Electron validation on 2026-05-15 verified readability and click-through behavior for the fixed compact bubble. | Covered for the v0.2 compact-bubble scope; a scrollable or growing bubble is not part of this acceptance row. |
| Continuity presence loop | Covered | `apps/backend/app/services/continuity.py` keeps review-gated confirmed state, emits high-presence prompt context and confirmed-only runtime presence signals; `apps/backend/app/agents/events.py` defines `continuity_signal`; `apps/desktop/src/App.tsx` renders compact continuity hints and maps confirmed signals/state into pet stage `presence`/`reflective` display states; user-confirmed manual Electron validation on 2026-05-15 verified visible timing, wording, and presence behavior in the running app. | Covered for the v0.2 prompt/display scope; TTS mouth timing and Q-version sprite pet action orchestration remain future frontend work. |
| Private auto organization activity ledger | Covered | `apps/backend/app/services/agent_actions.py` implements automation policy, activity storage, automation settings, Markdown snapshots, and revert records; `apps/backend/app/api/agent.py` exposes `GET /api/agent/actions` and `POST /api/agent/actions/{action_id}/revert`; `apps/backend/app/agents/events.py` defines SSE `agent_action`; `apps/backend/tests/test_agent_actions.py` and `apps/backend/tests/test_api_wiring_mvp.py::test_chat_stream_auto_archives_daily_memory_and_records_action` cover policy, settings, ledger, revert, and chat-triggered activity. | High-risk confirmation remains mandatory; low-risk actions are shown as recent activity and reversible Markdown writes expose undo. |
| Post-chat diary to Wiki distillation | Covered | `apps/backend/app/api/chat.py` archives successful answers to daily/structured diary and then calls `apps/backend/app/services/chat_answer_wiki_summary.py` to skip low-value/sensitive content or write useful summaries to `Wiki/Companion/Summaries/*.md`; `wiki.answer_summary.write` uses `AutomationPolicy`, Markdown snapshots, `Wiki/index.md`, `Wiki/log.md`, and SSE `agent_action`; `apps/backend/tests/test_chat_auto_memory_services.py` and `apps/backend/tests/test_api_wiring_mvp.py::test_chat_stream_auto_summarizes_useful_answer_to_wiki` cover the loop. | Deterministic summarization is conservative; a future model summarizer can improve content quality but must preserve the same safety gates. |
| Wiki workflow surface | Covered | Backend exposes low-risk automatic Wiki organization through `wiki_manager_agent` plus guarded ingest preview/review/apply, query archive list/detail, lint, core status, and synthesize routes; `apps/backend/app/services/wiki.py` default schema documents source boundaries, the fixed 8-section template, evidence/trigger source, self-check, logs/versioning, query workflow, and terminology/format traps; `apps/backend/app/services/wiki_lint.py` flags missing template/source/trigger/revision/log/inbound-link/format-trap issues; `apps/desktop/src/services/desktopApi.ts` has matching methods; `apps/desktop/src/App.tsx` shows ordinary writes as recent automatic activity and keeps advanced preview/review/apply controls for high-risk/manual maintenance; user-confirmed manual Vault/Wiki E2E on 2026-05-15 verified proposal review, non-ingest apply, index/log updates, and Obsidian browsing on an isolated or backed-up Vault. | Future manual trials should add explicit low-risk auto-summary write + revert observation on an isolated/backed-up Vault. |
| Dual-track memory runtime | Covered | `apps/backend/app/agents/immediate_understanding.py`, `apps/backend/app/services/memory_consolidation.py`, `apps/backend/app/services/memory_activation.py`, `apps/backend/app/services/memory_permissions.py`, `apps/backend/app/services/memory_lifecycle.py`, `apps/backend/app/services/memory_hygiene.py`, and `apps/backend/app/agents/graph_runtime.py` wire immediate state-only understanding, conservative slow consolidation, activation scoring, recall permissions, lifecycle transitions, feedback, hygiene, and main chat retrieval/consolidation; automated evidence includes `apps/backend/tests/test_memory_taxonomy.py`, `apps/backend/tests/test_dual_track_memory_schema.py`, `apps/backend/tests/test_immediate_understanding.py`, `apps/backend/tests/test_memory_consolidation.py`, `apps/backend/tests/test_memory_lifecycle.py`, `apps/backend/tests/test_memory_activation.py`, `apps/backend/tests/test_memory_permissions.py`, `apps/backend/tests/test_memory_feedback_api.py`, `apps/backend/tests/test_memory_hygiene.py`, `apps/backend/tests/test_memory_pollution_regression.py`, `apps/backend/tests/test_agent_runtime_chat.py`, `apps/backend/tests/test_agent_runtime_retrieval.py`, and `apps/backend/tests/test_api_wiring_mvp.py`; Task 15 full backend verification passed with `645 passed, 2 skipped`. | Covered for backend/runtime behavior: immediate understanding can affect the current reply without durable writes; slow consolidation records candidates/facts with permissions, lifecycle, activation, feedback, and audit evidence; pollution regressions cover one-off emotions, jokes, model summaries, complaining labels, boundaries, archived projects, user correction, sensitive content, and automatic action traces. |
| Weekly memory review surface | Partial | Backend/API and desktop wiring exist through `apps/backend/app/api/memory.py`, `apps/desktop/src/services/desktopApi.ts`, and `apps/desktop/src/views/MemoryWindowView.tsx`; automated evidence includes `apps/backend/tests/test_memory_feedback_api.py`, `apps/desktop/src/views/MemoryWindowView.test.tsx`, Task 15 `npm run typecheck`, and Task 15 Electron migration validation. | Backend/API/TypeScript coverage supports kept/temporary/ignored summaries and lightweight actions such as keep, edit, forget, only-this-week, and mark-completed, but Task 15 did not run a manual Electron visual trial, so desktop visual fit and ergonomics remain a residual manual check. |

## Automated Checks Added

- `scripts/check-mvp-acceptance-gap.ps1` verifies that the matrix exists, required statuses are documented, and key implementation signals still match the stated gaps.
- `apps/backend/tests/test_mvp_acceptance_gap_contract.py` mirrors the same contract in pytest so backend validation catches accidental documentation drift.
- `apps/desktop/scripts/validate-sprite-pet-assets.mjs` validates the sprite-pet halfbody manifest and layered portrait PNG dimensions for public and build output resources.
- `apps/desktop/scripts/validate-pet-bubble-pagination.mjs` validates desktop-pet reply pagination semantics for the current compact bubble behavior.
- `apps/desktop/scripts/validate-electron-migration.mjs` verifies the Electron migration baseline and rejects renderer `localStorage` usage; the current renderer stores allowed UI state through the Electron preload bridge with a `sessionStorage` browser-development fallback.
- `apps/backend/tests/test_memory_pollution_regression.py` locks the dual-track memory pollution scenarios: one-off emotion, jokes, model summaries, repeated complaining, explicit boundaries, completed/archived projects, related new projects, user correction, sensitive content, and automatic action traceability.
- `apps/backend/tests/test_memory_taxonomy.py`, `apps/backend/tests/test_dual_track_memory_schema.py`, `apps/backend/tests/test_memory_consolidation.py`, `apps/backend/tests/test_memory_lifecycle.py`, `apps/backend/tests/test_memory_activation.py`, `apps/backend/tests/test_memory_permissions.py`, `apps/backend/tests/test_memory_feedback_api.py`, and `apps/backend/tests/test_memory_hygiene.py` cover the slow memory contract, lifecycle, recall permissions, feedback, and cleanup behavior.
