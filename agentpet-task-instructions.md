# agentpet 重构任务指令集（Agent 可执行版）

> 命名规范：`TASK-[阶段编号][序号]`，例如 `TASK-0001` 表示阶段 0 第 1 个任务。
> 每个任务包含：目标 / 前置依赖 / 执行步骤 / 验收标准（DoD） / 输出物 / 建议执行角色。
> 任务必须按阶段顺序执行，**同阶段内任务可并行，跨阶段不可跳过**。任何任务在验收标准未满足前，不得标记为 `Completed`，只能标记为 `In Progress` 或 `Blocked`。

---

## 阶段 0：止血（环境与仓库卫生）

### TASK-0001：清理仓库临时文件与本机残留路径
- **目标**：消除仓库中混入的本机临时产物，恢复仓库可信度。
- **前置依赖**：无
- **执行步骤**：
  1. 删除 `pytest-of-*/` 目录及其全部内容。
  2. 在 `.gitignore` 中新增规则：`pytest-of-*/`、`.pytest_cache/`、`*.pyc`、`__pycache__/`。
  3. 全仓库搜索绝对路径字符串（如硬编码仓库盘符、用户主目录或本机用户名），逐一替换为相对路径或环境变量配置。
  4. 提交 commit，message 格式：`chore: remove local artifacts and harden gitignore`。
- **验收标准（DoD）**：
  - [ ] 仓库中不存在任何 `pytest-of-*` 目录
  - [ ] `.gitignore` 包含上述规则
  - [ ] 全仓库搜索本机绝对路径字符串，结果为 0
- **输出物**：清理后的仓库 + 更新后的 `.gitignore`
- **建议执行角色**：`backend-worker`

---

### TASK-0002：补全仓库元信息
- **目标**：让仓库具备基本的可发现性和身份信息。
- **前置依赖**：无
- **执行步骤**：
  1. 在 GitHub 仓库设置中填写 `description`。
  2. 添加 `topics`：`desktop-pet`、`local-first`、`ai-companion`、`obsidian`（可调整）。
  3. 确认仓库已有合适的 `LICENSE` 文件，若无则补充。
- **验收标准（DoD）**：
  - [ ] GitHub "About" 区域不再显示 "No description, website, or topics provided"
  - [ ] 仓库根目录存在 `LICENSE` 文件
- **输出物**：更新后的仓库元信息
- **建议执行角色**：`docs-worker`

---

### TASK-0003：项目命名与定位决策
- **目标**：解决与同名热门项目（监控 AI coding agent 的桌宠）混淆的问题。
- **前置依赖**：无
- **执行步骤**：
  1. 评估是否更名（建议候选：`vault-pet`、`memorypet`、`local-companion-pet`）。
  2. 若决定更名，更新仓库名、README 标题、CHANGELOG、所有内部文档引用。
  3. 若决定不更名，在 README 顶部明确加一行声明区分定位，避免搜索混淆。
- **验收标准（DoD）**：
  - [ ] 命名决策已写入 `docs/decisions/naming.md`（新建该文件）
  - [ ] 若更名，全仓库无遗留旧名称引用
- **输出物**：命名决策记录文档
- **建议执行角色**：人工决策（不可由 agent 自行决定，需人类确认后再执行）

---

### TASK-0004：精简 progress.md
- **目标**：把"协作纪实"与"状态台账"分离，恢复 progress.md 的可读性。
- **前置依赖**：无
- **执行步骤**：
  1. 新建 `docs/archive/progress-history.md`，把现有 progress.md 中所有详细协作记录迁移过去并保留原始内容。
  2. 重写 `progress.md`，只保留：当前版本号、当前能力边界（Covered / Partial / Gap）、最近 5 条里程碑、下一步方向。
  3. 在新 progress.md 顶部加一行链接指向归档文件。
- **验收标准（DoD）**：
  - [ ] 新 progress.md 长度控制在一屏可读完（约 100 行以内）
  - [ ] 历史记录完整迁移，无内容丢失
- **输出物**：精简后的 `progress.md` + 归档文件
- **建议执行角色**：`docs-worker`

---

## 阶段 1：核心闭环验证（必须人工通过）

> ⚠️ 本阶段所有任务的验收标准中，**禁止仅以 agent 自测的 pytest 通过数作为完成依据**，必须有人工操作记录（截图/录屏/真实日志）。

### TASK-0101：核心交互链路真人验证——桌宠入口
- **目标**：证明"双击 Pet 窗口打开主舞台"这一核心入口真实可用。
- **前置依赖**：TASK-0001
- **执行步骤**：
  1. 在干净环境（新机器或新用户账号）从零安装 Electron 桌面端。
  2. 记录安装过程每一步是否符合预期，异常情况单独记录。
  3. 执行双击桌宠打开主舞台操作，全程录屏。
  4. 将录屏文件和操作记录存入 `docs/verification/task-0101/`。
- **验收标准（DoD）**：
  - [ ] 存在可回放的录屏文件，证明操作成功
  - [ ] README 中"当前验证边界"章节更新，移除"未完成可视确认"的表述（如确实通过）
- **输出物**：录屏文件 + 验证记录文档
- **建议执行角色**：人工执行，agent 仅可协助记录文档

---

### TASK-0102：核心交互链路真人验证——聊天与记忆闭环
- **目标**：验证"聊天 → 记忆归档 → 查看 → 撤回"完整链路在真实数据路径下可用。
- **前置依赖**：TASK-0101
- **执行步骤**：
  1. 使用真实（非 mock）Vault 备份环境，发起一次真实对话。
  2. 确认对话触发记忆归档，记录归档内容与时间戳。
  3. 在记忆页查看该条记忆，确认展示信息（来源、原因、风险等级）准确。
  4. 执行撤回操作，确认底层数据库/文件状态同步更新（附数据库查询截图）。
  5. 至少记录 1 个失败案例（如有），不得只记录成功路径。
- **验收标准（DoD）**：
  - [ ] 全链路操作有真实日志和截图证据
  - [ ] 撤回后的底层数据状态已核实并记录
  - [ ] 验证记录（包括失败案例）存入 `docs/verification/task-0102/`
- **输出物**：端到端验证记录
- **建议执行角色**：人工执行 + `backend-worker` 配合取数据库证据

---

### TASK-0103：非编写者参与的可用性验证
- **目标**：引入非项目开发者的真实用户视角验证。
- **前置依赖**：TASK-0101、TASK-0102
- **执行步骤**：
  1. 邀请 1-2 名不了解代码、非 AI agent 的真人测试者。
  2. 提供一份不含开发者解释的纯操作手册，让其独立完成核心闭环操作。
  3. 记录测试者的卡点、误解、放弃环节。
- **验收标准（DoD）**：
  - [ ] 至少 1 名非开发者独立完成核心闭环操作并留下反馈记录
  - [ ] 卡点清单已整理并转化为后续待修复任务
- **输出物**：可用性测试报告
- **建议执行角色**：人工执行

---

## 阶段 2：范围收缩

### TASK-0201：模块优先级评审与冻结清单制定
- **目标**：明确当前阶段允许推进的模块范围，冻结其余模块。
- **前置依赖**：阶段 1 全部任务完成
- **执行步骤**：
  1. 对四条产品线（桌宠基础体验 / 记忆可控性 / Obsidian Wiki 集成 / 连续性引擎与 Live2D 高级表现）逐一评估：去掉该模块产品核心承诺是否仍成立。
  2. 将评估结果写入 `docs/decisions/scope-freeze.md`，明确本阶段允许推进与冻结的模块清单。
  3. 在 `task.md` 中标记所有冻结模块对应的任务为 `Frozen`，并注明解冻条件。
- **验收标准（DoD）**：
  - [ ] 评审文档存在且包含明确的允许/冻结清单
  - [ ] `task.md` 中所有冻结任务状态已更新
- **输出物**：范围冻结决策文档
- **建议执行角色**：`coordinator`（需人工确认最终清单）

---

### TASK-0202：长期记忆查看与撤回体验深度打磨
- **目标**：把记忆可控性做成产品的核心差异化能力。
- **前置依赖**：TASK-0201
- **执行步骤**：
  1. 优化记忆列表的展示逻辑：来源、原因、风险等级、可信度需清晰可读。
  2. 实现一键导出记忆为可迁移格式（如 JSON/Markdown），用于支撑跨工具可迁移性。
  3. 实现撤回操作的二次确认与撤回结果的可视反馈。
- **验收标准（DoD）**：
  - [ ] 记忆导出功能可用并经过人工验证
  - [ ] 撤回操作有清晰的成功/失败反馈
- **输出物**：记忆管理模块更新
- **建议执行角色**：`backend-worker` + 前端对接

---

## 阶段 3：验证体系重建

### TASK-0301：建立分层验证标准并写入开发规范
- **目标**：终结"agent 自证清白"的验证模式。
- **前置依赖**：阶段 1 完成
- **执行步骤**：
  1. 在 `docs/verification-policy.md` 中定义 L1-L4 验证层级（单元测试 / 契约测试 / 真实路径端到端测试 / 真人可用性验证）。
  2. 规定：仅 L1 通过的功能只能标记为 `Implemented (unverified)`，不可标记为 `Completed`。
  3. 更新 `progress.md` 模板，新增"验证层级"字段。
- **验收标准（DoD）**：
  - [ ] 验证政策文档已创建并合并入主文档体系
  - [ ] progress.md 模板已更新且团队（含 agent 协作流程）已遵循
- **输出物**：验证政策文档
- **建议执行角色**：`docs-worker`

---

## 阶段 4：文档与版本治理

### TASK-0401：合并冲突文档，消灭"双规格"债务
- **目标**：消除 `Development_Documentation.md` 与 `docs/` 并存且声明"冲突时以代码为准"的债务状态。
- **前置依赖**：无（可与阶段 0 并行）
- **执行步骤**：
  1. 逐条比对 `Development_Documentation.md` 与当前 `docs/` 规格差异。
  2. 以当前代码行为为基准，合并为一份唯一权威规格文档。
  3. 将 `Development_Documentation.md` 移入 `docs/archive/`，并在文件头部加注"已归档，不再维护，发布日期：[日期]"。
- **验收标准（DoD）**：
  - [ ] 仓库中只有一份当前生效的规格文档
  - [ ] 旧文档已归档并加注说明
- **输出物**：合并后的规格文档
- **建议执行角色**：`docs-worker`

---

### TASK-0402：版本号语义化修正
- **目标**：避免版本号给人"已是可用产品"的错误印象。
- **前置依赖**：阶段 1 未完成前必须执行本任务
- **执行步骤**：
  1. 检查当前版本号（如 `0.2.0`），若阶段 1 尚未完成，降级标注为 `0.0.x-alpha` 或 `dev`。
  2. 更新 `CHANGELOG.md`，区分"新增功能 / 修复 / 已知限制"三类条目，不与测试通过数混写。
- **验收标准（DoD）**：
  - [ ] 版本号已按实际完成度修正
  - [ ] CHANGELOG 格式已统一为三段式分类
- **输出物**：修正后的版本标识与 CHANGELOG
- **建议执行角色**：`coordinator`

---

## 阶段 5：留存与商业价值设计（新增，关键阶段）

> 本阶段任务对应"用户是否会持续使用"的核心判断，**不可跳过，且优先级高于阶段 2 中除核心体验外的所有模块**。

### TASK-0501：重写产品主叙事，移除"本地化"作为核心卖点
- **目标**：纠正"本地优先=隐私安全"这一经不起拆穿的营销叙事。
- **前置依赖**：无（建议尽早执行）
- **执行步骤**：
  1. 重写 README 产品体验章节，主叙事改为"记忆透明可控 + 跨工具可迁移"。
  2. 明确披露：对话内容会发送至所选 LLM API，本地保存的是结构化记忆衍生品，不等于对话内容不出本机。
  3. 移除或弱化所有暗示"对话内容完全不离开本机"的表述。
- **验收标准（DoD）**：
  - [ ] README 中不存在误导性隐私表述
  - [ ] 新增"数据流向说明"小节，清晰图示对话内容与记忆数据的不同流转路径
- **输出物**：更新后的 README
- **建议执行角色**：`docs-worker`

---

### TASK-0502：实现"本地隐私模式"开关（真实功能，非话术）
- **目标**：把隐私故事从营销话术变成可验证的真实功能。
- **前置依赖**：TASK-0501
- **执行步骤**：
  1. 设计并实现一个用户可选开关：开启后，敏感内容仅做本地关键词匹配记忆，不发送至云端 API。
  2. 在 UI 上明确告知开关的能力取舍（隐私 vs 智能程度）。
  3. 编写对应测试用例验证开关生效后网络请求确实未发出。
- **验收标准（DoD）**：
  - [ ] 开关功能通过网络层验证（如抓包确认开启后无相关 API 请求）
  - [ ] UI 文案清晰说明取舍
- **输出物**：隐私模式功能模块
- **建议执行角色**：`backend-worker`

---

### TASK-0503：记忆召回"恰到好处感"调优
- **目标**：避免记忆召回沦为机械检索拼接，造成"被识破"的信任崩塌。
- **前置依赖**：TASK-0102
- **执行步骤**：
  1. 收集至少 20 组真实对话样本，标注"召回是否恰当"（过度、不足、生硬、自然）。
  2. 调整召回触发时机与措辞模板，避免直接回显原始记忆数据。
  3. 设计召回效果的人工评分流程，纳入常规迭代。
- **验收标准（DoD）**：
  - [ ] 存在标注样本集与评分记录
  - [ ] 召回措辞模板已更新并通过至少一轮人工评审
- **输出物**：召回调优记录与样本集
- **建议执行角色**：`backend-worker` + 人工评审

---

### TASK-0504：设计桌宠"成长可见性"反馈机制
- **目标**：让用户能感知到自己的陪伴行为正在具体地改变桌宠。
- **前置依赖**：TASK-0201
- **执行步骤**：
  1. 设计至少 3 个可感知的成长维度（如外观变化、台词风格变化、互动解锁）。
  2. 将成长维度与记忆系统的真实数据挂钩（而非随机或纯时间触发）。
  3. 实现一个"成长记录"页面，用户可回看自己如何影响了桌宠。
- **验收标准（DoD）**：
  - [ ] 至少 1 个成长维度已实现并可通过真实使用触发
  - [ ] 成长记录页面可正确展示历史变化
- **输出物**：成长反馈机制模块
- **建议执行角色**：`backend-worker` + 前端对接

---

### TASK-0505：设计低摩擦日常触发机制（habit loop）
- **目标**：让桌宠嵌入用户已有的生活节奏，制造主动回访理由。
- **前置依赖**：TASK-0504
- **执行步骤**：
  1. 设计桌宠主动开口的触发规则（频率上限、合适时机、内容类型）。
  2. 避免打扰型设计，确保触发内容对用户有具体价值或情感价值。
  3. 增加用户可调节触发频率的设置项。
- **验收标准（DoD）**：
  - [ ] 主动触发功能已实现且有频率控制
  - [ ] 用户可在设置中调整触发频率
- **输出物**：日常触发机制模块
- **建议执行角色**：`backend-worker`

---

### TASK-0506：七天留存测试
- **目标**：用真实数据验证产品是否具备持续使用价值，而非仅验证功能可用性。
- **前置依赖**：TASK-0101、TASK-0102、TASK-0504、TASK-0505
- **执行步骤**：
  1. 招募至少 3-5 名真实测试者，连续使用产品 7 天。
  2. 每日记录是否主动打开产品、停留时长、主动触发的互动次数。
  3. 第 7 天进行访谈，记录"还愿不愿意继续用"及具体原因。
- **验收标准（DoD）**：
  - [ ] 完成至少 3 名测试者的 7 天完整数据记录
  - [ ] 测试报告包含留存率与定性反馈，存入 `docs/verification/task-0506/`
- **输出物**：七天留存测试报告
- **建议执行角色**：人工执行

---

## 任务执行总览表

| 任务编号 | 阶段 | 是否阻塞后续阶段 | 是否需要人工参与（不可纯 agent 完成） |
|---|---|---|---|
| TASK-0001 | 0 | 是 | 否 |
| TASK-0002 | 0 | 否 | 否 |
| TASK-0003 | 0 | 否 | 是（命名决策） |
| TASK-0004 | 0 | 否 | 否 |
| TASK-0101 | 1 | 是 | 是 |
| TASK-0102 | 1 | 是 | 是 |
| TASK-0103 | 1 | 是 | 是 |
| TASK-0201 | 2 | 是 | 是（最终确认） |
| TASK-0202 | 2 | 否 | 部分 |
| TASK-0301 | 3 | 否 | 否 |
| TASK-0401 | 4 | 否 | 否 |
| TASK-0402 | 4 | 否 | 否 |
| TASK-0501 | 5 | 否 | 否 |
| TASK-0502 | 5 | 否 | 否 |
| TASK-0503 | 5 | 否 | 是（评审） |
| TASK-0504 | 5 | 否 | 部分 |
| TASK-0505 | 5 | 否 | 否 |
| TASK-0506 | 5 | 是（决定项目是否继续投入） | 是 |

---

## 执行铁律（写给负责调度的 Coordinator agent）

1. 任何任务标记 `Completed` 前，必须核对该任务的 DoD 清单是否全部勾选，缺一项即保持 `In Progress`。
2. 标注"是否需要人工参与"为"是"的任务，agent 不得自行模拟验证结果替代人工执行。
3. 阶段 1 未全部完成前，禁止启动阶段 2 中除 TASK-0201 评审之外的任何新功能开发任务。
4. 每完成一个任务，必须同步更新 `progress.md` 中对应条目的验证层级（L1-L4），不得只写"测试通过"。
